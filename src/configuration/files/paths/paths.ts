export const JS_PATHS = 'jsconfig.json';

export const TS_PATHS = 'paths.json';

export const PATHS = {
  compilerOptions: { baseUrl: 'src' },
  include: ['src'],
  exclude: ['node_modules', 'public']
};
